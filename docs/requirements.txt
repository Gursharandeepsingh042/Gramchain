# GramChain — Python Requirements
# ML Service (FastAPI + scikit-learn + TensorFlow)
# Generated for: Python 3.11

# ─── Web Framework ────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1                    # For async HTTP calls to backend

# ─── Data & ML ────────────────────────────────────────────
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1
xgboost==2.1.3
imbalanced-learn==0.13.0         # SMOTE for class imbalance (rare defaults)
shap==0.47.0                     # Model explainability

# ─── Deep Learning (TFLite Export) ────────────────────────
tensorflow==2.18.0
tensorflow-decision-forests==1.10.0  # For tree-based TFLite models

# ─── Experiment Tracking ──────────────────────────────────
mlflow==2.20.2
optuna==4.2.0                    # Hyperparameter optimization

# ─── Data Validation ──────────────────────────────────────
pydantic==2.10.5
pydantic-settings==2.7.1

# ─── Serialization ────────────────────────────────────────
joblib==1.4.2                    # Model serialization
pickle5==0.0.12; python_version < "3.8"

# ─── Visualization (for notebooks) ────────────────────────
matplotlib==3.10.0
seaborn==0.13.2
plotly==5.24.1

# ─── Utilities ────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.3                    # Structured logging
redis==5.2.1                     # Cache connection
celery==5.4.0                    # Async task queue (optional)
pyarrow==18.1.0                  # Parquet for large datasets

# ─── Testing ──────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.25.2
pytest-cov==6.0.0
httpx==0.28.1                    # Test client for FastAPI

# ─── Code Quality ─────────────────────────────────────────
black==24.12.0
ruff==0.9.1
mypy==1.14.1
pre-commit==4.1.0

# ─── Jupyter (development only) ───────────────────────────
jupyter==1.1.1
ipykernel==6.29.5
notebook==7.3.2
